# Where the application backend lives.
#
# Left unset the app calls /api, which Vite proxies to http://localhost:8000 in
# development. Set this only if you serve the frontend somewhere that cannot
# proxy -- and then the backend needs CORS.
# VITE_API_BASE=/api
